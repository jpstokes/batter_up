To setup the app, execute the following commands at the command prompt:

ruby setup.rb --init
ruby import_data.rb

To run application, execute the following command at the command prompt:

ruby main.rb
